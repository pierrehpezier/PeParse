#!/usr/bin/env python

from distutils.core import setup

setup(name='PeParse',
      version='0.0.1',
      description='Python PE parser/generator',
      author='Pezier Pierre-Henri',
      py_modules = ['PE', 'COMPILER', 'INCLUDE'],
     )
